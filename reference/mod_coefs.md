# extract item parameters from a model record

extract item parameters from a model record

## Usage

``` r
mod_coefs(mod_rec, item_sep = "-")
```

## Arguments

- mod_rec:

  ModelRecord object

- item_sep:

  separator between item and instance index
