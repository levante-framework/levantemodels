# Get metadata for corpus items

Get metadata for corpus items

## Usage

``` r
fetch_corpus_items(version = "current")
```

## Examples

``` r
if (FALSE) { # \dontrun{
corpus_items <- fetch_corpus_items()
} # }
```
