# Get metadata for survey items

Get metadata for survey items

## Usage

``` r
fetch_survey_items(version = "current")
```

## Examples

``` r
if (FALSE) { # \dontrun{
survey_items <- fetch_survey_items()
} # }
```
