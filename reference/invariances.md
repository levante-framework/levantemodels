# mapping from invariance names to corresponding mirt argument

mapping from invariance names to corresponding mirt argument

## Usage

``` r
invariances
```

## Format

An object of class `list` of length 3.
